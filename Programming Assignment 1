1. Write a Python program to print "Hello Python".

Code:
print("Hello Python");

2. Write a Python program to do arithmetical operations addition and division.

num1=int(input('Enter first number:'));
num2=int(input('Enter second number:'));

print('Addition of num1 and num2 is=',(num1+num2));
print('Division of num1 aand num2 is=',(num1/num2));


## 3. Write a Python program to find the area of a triangle?
## area = height*base/2
Source code:

base=int(input('Enter base of triangle:'));
height=int(input('Enter height of triangle:'));
area = height*base/2;
print('Area of the triangle:',area);

O/P : Enter base of triangle:25
Enter height of triangle:100
Area of the triangle: 1250.0

 4. Write a Python program to swap two variables?
 
var1=int(input('Enter value of var1:'));
var2=int(input('Enter value of var2:'));

print('Before swapping value of var1 and var2 :',var1,var2);

temp=var1;
var1=var2;
var2=temp;

print('After swapping value of var1 and var2 :',var1,var2);


O/P : Enter value of var1:123
      Enter value of var2:111
Before swapping value of var1 and var2 : 123 111
After swapping value of var1 and var2 : 111 123

##  5. Write a Python program to generate a random number?

Source code:

import random
print(random.randint(0,9));
